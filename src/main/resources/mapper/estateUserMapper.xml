<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="houseway.houseway.repository.user.EstateRepository">


    <select id="selectFindUserEstate" parameterType="map">
        SELECT estate_id, estate_gu, estate_title, agent_num, estate_deposit, estate_rent
        FROM estate
        where ${findtype} like concat ('%', #{findkey}, '%')
        order by estate_id desc limit #{strnum}, #{pageSize}
    </select>

    <select id="countFindEstate" parameterType="map">
        SELECT count(estate_id) cnt FROM estate
        WHERE ${findtype} LIKE CONCAT('%', #{findkey}, '%')
    </select>
</mapper>